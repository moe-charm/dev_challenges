import 'package:flutter_test/flutter_test.dart';
import 'package:winner_simulator_app/main.dart';

void main() {
  test('WinnerSimulatorApp can be constructed', () {
    const app = WinnerSimulatorApp();
    expect(app, isA<WinnerSimulatorApp>());
  });
}
